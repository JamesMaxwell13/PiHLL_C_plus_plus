#include "MyString.h"
#include "MyString.cpp"

int main() {
//    char* buff = new char[256];
//    std::cin.getline(buff, 256);
//    char* pattern = new char[256];
//    std::cin.getline(pattern, 256);
    MyString a;
    std::cin >> a;
    std::cout << a;
    //delete a;
    //a *= 3;
//    if(a->value() == nullptr)
//    std::cout << "null";

//    int* index = a.find(pattern, strlen(pattern));
//    for(int i = 0; index[i] != 0; i++) {
//        std::cout << index[i] << std::endl;
//    }
    return 0;
}